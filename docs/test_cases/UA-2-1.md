<!--
此文件由 pytest 测试代码自动生成。
禁止直接修改。
-->

# UA-2-1

## UA-2-1-001 位号类型_一次位号新增

### 前置条件

- 数据源 alive=true；存在匹配的 *_r_* 节点

### 测试步骤

1. 使用 tagType=1 add_tag
2. 查询 tag 配置确认 tagType=1
3. 等待并读取 RT 与 quality
4. queryWithQuality 确认值一致

### 预期结果

- 新增成功
- tagType 精确为 1
- 配置字段与请求一致
- RT 可读取且 quality 有效
- RT 与源端值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_tag_type_read_only`

## UA-2-1-002 数据源_已组态运行

### 前置条件

- 数据源已创建并启用；Mock 运行；alive=true

### 测试步骤

1. 新增读取位号
2. 查询配置
3. 等待并读取实时值

### 预期结果

- add_tag 成功
- 位号配置字段正确
- getRTValue 与 queryWithQuality 均返回有效值
- 两入口值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_running`

## UA-2-1-003 数据源_未组态

### 前置条件

- TPT 中不存在目标 dsId

### 测试步骤

1. 使用不存在的 dsId 调用 add_tag
2. 按 tagName 查询

### 预期结果

- 请求被拒绝
- 返回明确错误
- 系统中不存在该位号

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_nonexistent_ds`

## UA-2-1-004 数据源_组态未启服务

### 前置条件

- 数据源已创建并启用；Mock 未运行；alive=false

### 测试步骤

1. 新增读取位号
2. 查询配置
3. 查询 RT 与 quality

### 预期结果

- 位号配置创建成功
- 配置字段正确
- RT 无有效采集值
- queryWithQuality quality=0

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_enabled_mocker_stopped`

## UA-2-1-005 数据源_未启服务后恢复

### 前置条件

- 数据源已创建并启用；Mock 初始未运行

### 测试步骤

1. 新增位号并确认 offline
2. 同端口启动 Mock
3. 等待 alive=true
4. 连续读取两次 RT
5. 读取 quality

### 预期结果

- 不需要重新新增位号
- 数据源恢复 alive=true
- RT 开始更新
- quality 从不可用恢复为有效值

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_offline_then_start_mocker`

## UA-2-1-006 数据源_组态已禁用

### 前置条件

- 数据源已创建但禁用；Mock 正常运行

### 测试步骤

1. 新增读取位号
2. 查询配置
3. 查询 RT 与 quality

### 预期结果

- 位号配置创建成功
- RT 无有效采集值
- queryWithQuality quality=0

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_disabled`

## UA-2-1-007 数据源_禁用后启用

### 前置条件

- 数据源禁用；位号已存在

### 测试步骤

1. 启用数据源
2. 等待 alive=true
3. 连续读取两次 RT

### 预期结果

- 不需要重新新增位号
- RT 开始更新
- quality 恢复为有效值

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_disabled_then_enable`

## UA-2-1-008 底层位号_规范格式

### 前置条件

- 数据源 alive=true；存在节点 ns=1, nodeId=double_ch_1

### 测试步骤

1. 新增 tagBaseName="1_double_ch_1"
2. 执行公共新增闭环

### 预期结果

- tagBaseName 原样保存
- RT 值与 asyncua 直读同一节点一致
- 质量有效

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_base_mapping.py::test_base_name_standard_format`

## UA-2-1-009 底层位号_不存在节点

### 前置条件

- 数据源 alive=true；源端不存在目标节点

### 测试步骤

1. 新增 tagBaseName="1_nonexistent"
2. 查询配置与实时值

### 预期结果

- 位号配置创建成功
- tagBaseName 正确保存
- RT 无有效值
- quality=0

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_base_mapping.py::test_base_name_nonexistent_node`

## UA-2-1-010 底层位号_跨数据源相同节点名

### 前置条件

- 两个数据源均 alive=true
- 各有 nodeId=shared_node_1，源端值不同

### 测试步骤

1. ds-A 新增 tag_A 指向 1_shared_node_1
2. ds-B 新增 tag_B 指向 1_shared_node_1
3. 分别读取 RT 和源端

### 预期结果

- 两个位号均创建成功
- 各自 RT 值等于各自数据源的源端值
- 两个位号不串值

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_base_mapping.py::test_base_name_cross_ds_same_node`

## UA-2-1-011 底层位号_同数据源重复映射

### 前置条件

- 同一数据源 alive=true；存在节点 2_smoke_static_1

### 测试步骤

1. 新增 tag_A 指向该节点
2. 新增 tag_B 也指向该节点
3. 读取两位号并确认独立

### 预期结果

- 第二次新增允许
- 两个 tag 各自读取独立 RT 值
- 原位号不受影响

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_base_mapping.py::test_base_name_duplicate_mapping`

## UA-2-1-012 底层位号_格式异常

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 使用 tagBaseName="invalid_format" 新增
2. 查询配置与 RT

### 预期结果

- 记录接口是否拒绝
- 若接受，记录字段保存值、RT 和质量
- 不得影响其他位号

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_base_mapping.py::test_base_name_invalid_format`

## UA-2-1-013 底层位号_Namespace不存在

### 前置条件

- 数据源 alive=true；Namespace 99 不存在

### 测试步骤

1. 新增 tagBaseName="99_double_ch_1"
2. 查询配置与质量

### 预期结果

- 配置可创建
- RT 无有效值
- quality=0

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_base_mapping.py::test_base_name_nonexistent_namespace`

## UA-2-1-014 底层位号_空值

### 前置条件

- 数据源 alive=true

### 测试步骤

1. tagBaseName="" 调用 add_tag
2. 按系统位号名查询

### 预期结果

- 请求被拒绝或按接口默认规则处理
- 若拒绝，不生成位号
- 若接受，记录行为

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_base_mapping.py::test_base_name_empty`

## UA-2-1-015 底层位号_配置类型与源端类型不一致

### 前置条件

- 源端节点为 Double；数据源 alive=true

### 测试步骤

1. 使用该节点新增 dataType=Boolean 位号
2. 查询配置、RT 和质量

### 预期结果

- 记录新增阶段是否拒绝
- 若接受，记录最终 dataType、RT 值和质量
- 不得产生进程异常

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_base_mapping.py::test_base_name_data_type_mismatch`

## UA-2-1-016 系统位号名_与底层名独立

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 新增 tagName=<unique>, tagBaseName=1_double_ch_1
2. 查询记录并读取 RT

### 预期结果

- tagName 与 tagBaseName 分别按请求保存
- RT 来自 double_ch_1
- 两字段互不覆盖
- RT 与 asyncua 源端值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_independent_from_base`

## UA-2-1-017 系统位号名_同数据源重复

### 前置条件

- 同一数据源已存在 tagName

### 测试步骤

1. 再次新增相同 tagName
2. 查询该名称全部记录

### 预期结果

- 第二次请求被拒绝
- 仅保留原记录
- 原记录字段未被覆盖

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_duplicate_same_ds`

## UA-2-1-018 系统位号名_跨数据源重复

### 前置条件

- ds-A 已存在 tagName

### 测试步骤

1. ds-B 新增相同 tagName
2. 查询全局记录

### 预期结果

- 请求被拒绝
- 全局仅存在 ds-A 原记录
- 原记录不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_duplicate_cross_ds`

## UA-2-1-019 系统位号名_空字符串

### 前置条件

- 数据源 alive=true

### 测试步骤

1. tagName="" 调用 add_tag
2. 查询空名和测试前缀

### 预期结果

- 请求失败
- 错误信息明确
- 不生成残留位号

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_empty`

## UA-2-1-020 系统位号名_纯空白

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 分别使用一个空格、多个空格、Tab 新增
2. 查询实际保存名称

### 预期结果

- 记录是否 trim 或拒绝
- 不得生成多个不可区分的空白名称
- 失败时不产生残留

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_whitespace`

## UA-2-1-021 系统位号名_长度边界127

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 使用 127 字符名称新增
2. 查询名称与长度

### 预期结果

- 请求结果明确
- 若成功，名称完整保存且长度为 127
- 不得静默产生乱码

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_length_127`

## UA-2-1-022 系统位号名_长度边界128

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 使用 128 字符名称新增
2. 查询名称与长度

### 预期结果

- 请求结果明确
- 若成功，名称完整保存且长度为 128
- 若拒绝，不生成记录

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_length_128`

## UA-2-1-023 系统位号名_长度边界129

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 使用 129 字符名称新增
2. 查询名称与长度
3. 检查与 128 对照是否冲突

### 预期结果

- 记录拒绝、截断或接受行为
- 不得静默截断后与已有名称冲突
- 不影响其他位号

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_length_129`

## UA-2-1-024 系统位号名_特殊字符

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 使用 a/b\c.d@e#f 新增
2. 查询实际保存名称并读取 RT

### 预期结果

- 记录允许字符范围
- 若成功，名称原样保存且可查询、可读取
- 若拒绝，不生成残留

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_special_chars`

## UA-2-1-025 系统位号名_Unicode与大小写

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 分别新增中文、Emoji、Tag_A、tag_a
2. 查询全部记录

### 预期结果

- 记录 Unicode 支持情况
- 记录大小写唯一性
- 查询结果与实际保存一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_system_name.py::test_system_name_unicode_and_case`

## UA-2-1-026 默认读取类型_Boolean

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 Boolean 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_boolean`

## UA-2-1-027 默认读取类型_SByte

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 SByte 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_sbyte`

## UA-2-1-028 默认读取类型_Byte

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 Byte 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_byte`

## UA-2-1-029 默认读取类型_Int16

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 Int16 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_int16`

## UA-2-1-030 默认读取类型_UInt16

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 UInt16 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_uint16`

## UA-2-1-031 默认读取类型_Int32

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 Int32 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_int32`

## UA-2-1-032 默认读取类型_UInt32

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 UInt32 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_uint32`

## UA-2-1-033 默认读取类型_Int64

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 Int64 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_int64`

## UA-2-1-034 默认读取类型_UInt64

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 UInt64 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_uint64`

## UA-2-1-035 默认读取类型_Float

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 Float 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_float`

## UA-2-1-036 默认读取类型_Double

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 Double 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_double`

## UA-2-1-037 默认读取类型_String

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 String 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_string`

## UA-2-1-038 默认读取类型_DateTime

### 前置条件

- 数据源 alive=true

### 测试步骤

1. 创建 DateTime 节点并新增位号
2. 等待第一次 RT 值
3. 等待第二次 RT 值发生变化
4. 联合轮询 RT 与 queryWithQuality 直到一致

### 预期结果

- dataType 与请求一致
- tagBaseName 与请求一致
- 两次 RT 值发生变化
- 两次 RT quality 有效
- 两次 RT tagTime 可严格解析
- 第二次 RT 时间不早于第一次
- queryWithQuality 返回唯一目标记录
- queryWithQuality quality 存在且有效
- queryWithQuality tagTime 存在且可解析
- queryWithQuality tagValue 存在
- queryWithQuality 与 getRTValue 值一致
- RT 与 asyncua 源端夹逼值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_default_reads.py::test_default_read_datetime`

## UA-2-1-039 Boolean 写入 true

### 前置条件

- 数据源 alive=true
- Boolean 可写节点初始为 false

### 测试步骤

1. 创建 Boolean 可写节点，初始 false
2. 新增位号 onlyRead=false
3. 确认 dataType=1, onlyRead=false
4. 保存 asyncua 源端原值
5. 调用 write_tag_values 写入 True
6. 轮询 asyncua 源端变为 True
7. 联合轮询 RT 与 queryWithQuality 均为 True
8. 验证两个入口 quality 有效、tagTime 可解析
9. finally 恢复源端为 False

### 预期结果

- 写接口响应成功
- 源端 Python 类型为 bool
- 源端值 is True
- RT 规范值 is True
- QwQ 规范值 is True
- quality 有效
- tagTime 可解析

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_boolean_writes.py::test_boolean_write_true`

## UA-2-1-040 Boolean 写入 false

### 前置条件

- 数据源 alive=true
- Boolean 可写节点初始为 true

### 测试步骤

1. 创建 Boolean 可写节点，初始 true
2. 新增位号 onlyRead=false
3. 确认 dataType=1, onlyRead=false
4. 保存 asyncua 源端原值
5. 调用 write_tag_values 写入 False
6. 轮询 asyncua 源端变为 False
7. 联合轮询 RT 与 queryWithQuality 均为 False
8. 验证两个入口 quality 有效、tagTime 可解析
9. finally 恢复源端为 True

### 预期结果

- 写接口响应成功
- 源端 Python 类型为 bool
- 源端值 is False
- RT 规范值 is False
- QwQ 规范值 is False
- quality 有效
- tagTime 可解析

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_boolean_writes.py::test_boolean_write_false`

## UA-2-1-041 Boolean 写入非布尔值

### 前置条件

- 数据源 alive=true
- Boolean 可写节点初始为 false

### 测试步骤

1. 创建 Boolean 可写节点，初始 false
2. 分别写入 1、0、"true"
3. 每个写入后记录完整 observation
4. 所有输入完成并清理后动态 xfail

### 预期结果

- 记录每个输入的被拒绝或转换行为
- 被拒绝时源端保持 False
- 被接受时源端仍为 bool
- quality 有效
- tagTime 可解析

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_boolean_writes.py::test_boolean_write_non_boolean_values`

## UA-2-1-042 SByte 最小值与最大值

### 前置条件

- 数据源 alive=true
- SByte 可写节点初始为 7

### 测试步骤

1. 写入 -128
2. 验证三端一致
3. 写入 127
4. 验证三端一致

### 预期结果

- 写响应成功
- 源端值正确
- VariantType 不变
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_8_16.py::test_sbyte_min_max`

## UA-2-1-043 SByte 越界值

### 前置条件

- 数据源 alive=true
- SByte 可写节点初始为 7

### 测试步骤

1. 写入 -129
2. 记录观察
3. 写入 128
4. 记录观察
5. 动态 XFAIL

### 预期结果

- 记录拒绝或转换行为
- 被拒绝时源端保持 7
- VariantType 不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_8_16.py::test_sbyte_out_of_range`

## UA-2-1-044 Byte 最小值与最大值

### 前置条件

- 数据源 alive=true
- Byte 可写节点初始为 7

### 测试步骤

1. 写入 0
2. 验证三端一致
3. 写入 255
4. 验证三端一致

### 预期结果

- 写响应成功
- 源端值正确
- VariantType 不变
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_8_16.py::test_byte_min_max`

## UA-2-1-045 Byte 越界值

### 前置条件

- 数据源 alive=true
- Byte 可写节点初始为 7

### 测试步骤

1. 写入 -1
2. 记录观察
3. 写入 256
4. 记录观察
5. 动态 XFAIL

### 预期结果

- 记录拒绝或转换行为
- 被拒绝时源端保持 7
- VariantType 不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_8_16.py::test_byte_out_of_range`

## UA-2-1-046 Int16 最小值与最大值

### 前置条件

- 数据源 alive=true
- Int16 可写节点初始为 123

### 测试步骤

1. 写入 -32768
2. 验证三端一致
3. 写入 32767
4. 验证三端一致

### 预期结果

- 写响应成功
- 源端值正确
- VariantType 不变
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_8_16.py::test_int16_min_max`

## UA-2-1-047 Int16 越界值

### 前置条件

- 数据源 alive=true
- Int16 可写节点初始为 123

### 测试步骤

1. 写入 -32769
2. 记录观察
3. 写入 32768
4. 记录观察
5. 动态 XFAIL

### 预期结果

- 记录拒绝或转换行为
- 被拒绝时源端保持 123
- VariantType 不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_8_16.py::test_int16_out_of_range`

## UA-2-1-048 UInt16 最小值与最大值

### 前置条件

- 数据源 alive=true
- UInt16 可写节点初始为 123

### 测试步骤

1. 写入 0
2. 验证三端一致
3. 写入 65535
4. 验证三端一致

### 预期结果

- 写响应成功
- 源端值正确
- VariantType 不变
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_8_16.py::test_uint16_min_max`

## UA-2-1-049 UInt16 越界值

### 前置条件

- 数据源 alive=true
- UInt16 可写节点初始为 123

### 测试步骤

1. 写入 -1
2. 记录观察
3. 写入 65536
4. 记录观察
5. 动态 XFAIL

### 预期结果

- 记录拒绝或转换行为
- 被拒绝时源端保持 123
- VariantType 不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_8_16.py::test_uint16_out_of_range`

## UA-2-1-050 Int32 最小值与最大值

### 前置条件

- 数据源 alive=true
- Int32 可写节点初始为 123456

### 测试步骤

1. 写入 -2147483648
2. 验证三端一致
3. 写入 2147483647
4. 验证三端一致

### 预期结果

- 写响应成功
- 源端值正确
- VariantType 不变
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_32.py::test_int32_min_max`

## UA-2-1-051 Int32 越界值

### 前置条件

- 数据源 alive=true
- Int32 可写节点初始为 123456

### 测试步骤

1. 写入 -2147483649
2. 记录观察
3. 写入 2147483648
4. 记录观察
5. 动态 XFAIL

### 预期结果

- 记录拒绝或转换行为
- 被拒绝时源端保持 123456
- VariantType 不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_32.py::test_int32_out_of_range`

## UA-2-1-052 UInt32 最小值与最大值

### 前置条件

- 数据源 alive=true
- UInt32 可写节点初始为 123456

### 测试步骤

1. 写入 0
2. 验证三端一致
3. 写入 4294967295
4. 验证三端一致

### 预期结果

- 写响应成功
- 源端值正确
- VariantType 不变
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_32.py::test_uint32_min_max`

## UA-2-1-053 UInt32 越界值

### 前置条件

- 数据源 alive=true
- UInt32 可写节点初始为 123456

### 测试步骤

1. 写入 -1
2. 记录观察
3. 写入 4294967296
4. 记录观察
5. 动态 XFAIL

### 预期结果

- 记录拒绝或转换行为
- 被拒绝时源端保持 123456
- VariantType 不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_32.py::test_uint32_out_of_range`

## UA-2-1-054 Int64 JS 安全范围值

### 前置条件

- 数据源 alive=true
- Int64 可写节点初始为 123456

### 测试步骤

1. 写入 9999999999
2. 验证三端一致

### 预期结果

- 写响应成功
- 源端精确为 9999999999
- VariantType 保持 Int64
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_64.py::test_int64_js_safe_value`

## UA-2-1-055 Int64 最小值与最大值

### 前置条件

- 数据源 alive=true
- Int64 可写节点初始为 123456

### 测试步骤

1. 写入 -9223372036854775808
2. 验证三端一致
3. 写入 9223372036854775807
4. 验证三端一致

### 预期结果

- 写响应成功
- 源端值正确
- VariantType 不变
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_64.py::test_int64_min_max`

## UA-2-1-056 Int64 越界值

### 前置条件

- 数据源 alive=true
- Int64 可写节点初始为 123456

### 测试步骤

1. 写入 -9223372036854775809
2. 记录观察
3. 写入 9223372036854775808
4. 记录观察
5. 动态 XFAIL

### 预期结果

- 记录拒绝或转换行为
- 被拒绝时源端保持 123456
- VariantType 不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_64.py::test_int64_out_of_range`

## UA-2-1-057 UInt64 JS 安全范围值

### 前置条件

- 数据源 alive=true
- UInt64 可写节点初始为 123456

### 测试步骤

1. 写入 9999999999
2. 验证三端一致

### 预期结果

- 写响应成功
- 源端精确为 9999999999
- VariantType 保持 UInt64
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_uint64.py::test_uint64_js_safe_value`

## UA-2-1-058 UInt64 最大值

### 前置条件

- 数据源 alive=true
- UInt64 可写节点初始为 123456

### 测试步骤

1. 写入 18446744073709551615
2. 验证三端一致

### 预期结果

- 写响应成功
- 源端值正确
- VariantType 不变
- RT/QwQ 一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_uint64.py::test_uint64_max_value`

## UA-2-1-059 UInt64 负数与越界值

### 前置条件

- 数据源 alive=true
- UInt64 可写节点初始为 123456

### 测试步骤

1. 写入 -1
2. 记录观察
3. 写入 18446744073709551616
4. 记录观察
5. 动态 XFAIL

### 预期结果

- 记录拒绝或转换行为
- 被拒绝时源端保持 123456
- VariantType 不变

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_integer_writes_uint64.py::test_uint64_negative_and_out_of_range`

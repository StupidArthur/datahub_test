<!--
此文件由 pytest 测试代码自动生成。
禁止直接修改。
-->

# UA-2-1

## UA-2-1-001 位号类型_一次位号新增

### 前置条件

- 数据源 alive=true；存在匹配的 *_r_* 节点

### 测试步骤

1. 使用 tagType=1 add_tag
2. 查询 tag 配置确认 tagType=1
3. 等待并读取 RT 与 quality
4. queryWithQuality 确认值一致

### 预期结果

- 新增成功
- tagType 精确为 1
- 配置字段与请求一致
- RT 可读取且 quality 有效
- RT 与源端值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_tag_type_read_only`

## UA-2-1-002 数据源_已组态运行

### 前置条件

- 数据源已创建并启用；Mock 运行；alive=true

### 测试步骤

1. 新增读取位号
2. 查询配置
3. 等待并读取实时值

### 预期结果

- add_tag 成功
- 位号配置字段正确
- getRTValue 与 queryWithQuality 均返回有效值
- 两入口值一致

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_running`

## UA-2-1-003 数据源_未组态

### 前置条件

- TPT 中不存在目标 dsId

### 测试步骤

1. 使用不存在的 dsId 调用 add_tag
2. 按 tagName 查询

### 预期结果

- 请求被拒绝
- 返回明确错误
- 系统中不存在该位号

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_nonexistent_ds`

## UA-2-1-004 数据源_组态未启服务

### 前置条件

- 数据源已创建并启用；Mock 未运行；alive=false

### 测试步骤

1. 新增读取位号
2. 查询配置
3. 查询 RT 与 quality

### 预期结果

- 位号配置创建成功
- 配置字段正确
- RT 无有效采集值
- queryWithQuality quality=0

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_enabled_mocker_stopped`

## UA-2-1-005 数据源_未启服务后恢复

### 前置条件

- 数据源已创建并启用；Mock 初始未运行

### 测试步骤

1. 新增位号并确认 offline
2. 同端口启动 Mock
3. 等待 alive=true
4. 连续读取两次 RT
5. 读取 quality

### 预期结果

- 不需要重新新增位号
- 数据源恢复 alive=true
- RT 开始更新
- quality 从不可用恢复为有效值

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_offline_then_start_mocker`

## UA-2-1-006 数据源_组态已禁用

### 前置条件

- 数据源已创建但禁用；Mock 正常运行

### 测试步骤

1. 新增读取位号
2. 查询配置
3. 查询 RT 与 quality

### 预期结果

- 位号配置创建成功
- RT 无有效采集值
- queryWithQuality quality=0

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_disabled`

## UA-2-1-007 数据源_禁用后启用

### 前置条件

- 数据源禁用；位号已存在

### 测试步骤

1. 启用数据源
2. 等待 alive=true
3. 连续读取两次 RT

### 预期结果

- 不需要重新新增位号
- RT 开始更新
- quality 恢复为有效值

**pytest nodeid**: `tests/integration/ua2/test_tag_creation_datasource_state.py::test_ds_disabled_then_enable`

<!--
此文件由 pytest 测试代码自动生成。
禁止直接修改。
-->

# UA-1-3

## UA-1-3-01 断开后各项指标变化延迟

### 前置条件

- mock 配置 change=true；alive=true；位号正常采集

### 测试步骤

1. 连续 getRTValue 2 次确认值在变化
2. 记录历史当前条数
3. 停止 mock，记录 t0
4. 每 1s 轮询以下指标，记录各自发生变化的时刻：a) alive 变 false  b) RT quality 变 0 / RT 不可读
5. 4 个指标最终都应发生变化

### 预期结果

- alive 在合理时间内变 false
- RT 查询最终抛 TptAPIError(tag 不存在)

**pytest nodeid**: `tests/integration/ua1/test_recovery_and_reconnect.py::test_disconnect_detection_latency`

## UA-1-3-02 重连后各项指标恢复延迟

### 前置条件

- UA-1-3-01 之后：mock 已停止；数据源仍为 enabled；alive=false

### 测试步骤

1. 启动 mock（同端口同配置），记录 t0
2. 每 1s 轮询：a) alive 变 true  b) RT quality 恢复非0

### 预期结果

- alive 在合理时间内变 true
- RT 恢复有效值

**pytest nodeid**: `tests/integration/ua1/test_recovery_and_reconnect.py::test_reconnect_recovery_latency`

## UA-1-3-06 短暂断连恢复

### 前置条件

- mock 配置 change=true；alive=true

### 测试步骤

1. 停止 mock
2. 立即重启（<5s 内）
3. 等待 2 个采集周期

### 预期结果

- alive 恢复 true
- RT 采集恢复
- 无数据断层

**pytest nodeid**: `tests/integration/ua1/test_recovery_and_reconnect.py::test_short_disconnect_recovery`

## UA-1-3-07 长时间断连后恢复

### 前置条件

- mock 配置 change=true；alive=true

### 测试步骤

1. 停止 mock
2. 等待长窗口（本测试用 30s 替代规格的 120s 以保持 pytest 周期合理）
3. 重启 mock
4. 等待 2 个采集周期

### 预期结果

- alive 恢复 true
- RT 采集恢复
- 系统无崩溃、无资源泄漏

**pytest nodeid**: `tests/integration/ua1/test_recovery_and_reconnect.py::test_long_disconnect_recovery`

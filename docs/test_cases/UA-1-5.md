<!--
此文件由 pytest 测试代码自动生成。
禁止直接修改。
-->

# UA-1-5

## UA-1-5-01 删除_禁用+无位号

### 前置条件

- 数据源已注册，无位号，已禁用(alive=false)

### 测试步骤

1. delete_ds_info([id])
2. list_ds_info

### 预期结果

- 删除成功；数据源不再出现

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_delete_disabled_no_tags`

## UA-1-5-02 删除_禁用+有位号

### 前置条件

- 数据源已禁用，下有 N 个位号

### 测试步骤

1. delete_ds_info([id])
2. list_ds_info
3. list_tags(按原 dsId)

### 预期结果

- 验证能否直接删除；若不能，需先删位号；记录报错信息

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_delete_disabled_with_tags`

## UA-1-5-03 删除_启用+无位号

### 前置条件

- 数据源已启用(alive=true)，无位号

### 测试步骤

1. delete_ds_info([id])
2. list_ds_info

### 预期结果

- 删除成功或需先禁用；记录行为

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_delete_enabled_no_tags`

## UA-1-5-04 删除_启用+有位号

### 前置条件

- 数据源已启用，下有 N 个位号，正常采集

### 测试步骤

1. delete_ds_info([id])
2. list_ds_info
3. list_tags(按原 dsId)

### 预期结果

- 验证能否直接删除；若不能，需先禁用或删位号；记录报错信息

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_delete_enabled_with_tags`

## UA-1-5-05 删除_位号进回收站后删除数据源

### 前置条件

- 数据源下有位号；位号已软删（进入回收站）

### 测试步骤

1. delete_tags(软删)
2. 确认位号在回收站
3. delete_ds_info([id])
4. list_ds_info

### 预期结果

- 验证位号进回收站后数据源能否删除；若能删，回收站位号是否还在

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_delete_ds_after_tag_soft_delete`

## UA-1-5-06 删除后重建回收站位号残留

### 前置条件

- UA-1-5-05 数据源已删除，回收站有残留位号

### 测试步骤

1. add_ds_info(同 url)
2. list_recycle_tags

### 预期结果

- 验证回收站中的位号是否还存在；是否关联到新数据源

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_rebuild_recycle_tag_residual`

## UA-1-5-07 删除后同地址重建

### 前置条件

- 数据源已删除，mock 仍在运行

### 测试步骤

1. add_ds_info(同 url)
2. 启用
3. 注册位号
4. 等待采集

### 预期结果

- 新数据源注册成功；alive=true；位号正常采集

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_rebuild_same_endpoint`

## UA-1-5-08 删除不影响其他数据源

### 前置条件

- ds-A、ds-B 均正常

### 测试步骤

1. delete_ds_info([ds-A])
2. 验证 ds-B

### 预期结果

- ds-A 被删除
- ds-B 不受影响、alive=true、值继续变化

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_delete_does_not_affect_other_ds`

## UA-1-5-09 删除后位号 RT 状态

### 前置条件

- 数据源已删除，原位号仍存在（前置条件可能无法满足）

### 测试步骤

1. getRTValue 读原位号

### 预期结果

- 验证 RT 返回什么：报错、quality=0、还是返回最后值（若前置条件无法满足则标记 NA）

**pytest nodeid**: `tests/integration/ua1/test_datasource_deletion.py::test_rt_state_after_ds_delete`

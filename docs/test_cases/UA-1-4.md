<!--
此文件由 pytest 测试代码自动生成。
禁止直接修改。
-->

# UA-1-4

## UA-1-4-01 两数据源各自正常采集

### 前置条件

- mock-A、mock-B 均已启动，配置 change=true

### 测试步骤

1. 分别注册 ds-A、ds-B
2. 各注册位号
3. 等待采集
4. 各自 getRTValue 2 次确认值在变化

### 预期结果

- 两个 ds 均 alive=true
- 各自位号 RT 值 = 各自 mock 节点当前值

**pytest nodeid**: `tests/integration/ua1/test_multiple_datasources.py::test_two_sources_normal_collect`

## UA-1-4-02 一源断连不影响另一源

### 前置条件

- UA-1-4-01 状态

### 测试步骤

1. 停止 mock-A
2. 等待 2s
3. 验证 ds-A 和 ds-B

### 预期结果

- ds-A alive=false
- ds-B 不受影响、alive=true、值继续变化

**pytest nodeid**: `tests/integration/ua1/test_multiple_datasources.py::test_one_source_disconnect_other_untouched`

## UA-1-4-03 一源恢复不影响另一源

### 前置条件

- UA-1-4-02 后

### 测试步骤

1. 重启 mock-A
2. 等待重连
3. 验证 ds-A 和 ds-B

### 预期结果

- ds-A 恢复 alive=true、采集恢复
- ds-B 全程不受影响

**pytest nodeid**: `tests/integration/ua1/test_multiple_datasources.py::test_one_source_recover_other_untouched`

## UA-1-4-04 一源启停不影响另一源

### 前置条件

- UA-1-4-01 状态

### 测试步骤

1. 禁用 ds-A
2. 验证 ds-B
3. 启用 ds-A
4. 验证 ds-B

### 预期结果

- ds-B 全程 alive=true、值持续变化
- ds-A 启停对 ds-B 无影响

**pytest nodeid**: `tests/integration/ua1/test_multiple_datasources.py::test_one_source_disable_enable_other_untouched`

## UA-1-4-05 不同数据源相同底层位号

### 前置条件

- mock-A、mock-B 各有节点 ns=1, nodeid='test_wr_1'
- 两位号均可写，值稳定（change=false）以便验证写入值不回写冲突

### 测试步骤

1. ds-A 注册位号 tagName='tag_A'，tagBaseName='1_test_wr_1'
2. ds-B 注册位号 tagName='tag_B'，tagBaseName='1_test_wr_1'
3. 等待采集
4. 各自 writeTagValues 写不同值
5. 各自 getRTValue

### 预期结果

- 两个位号都注册成功、各自独立采集
- 各自 RT 值 = 各自写入值，互不串值
- 不同数据源下相同 ns+nodeid 可以并存

**pytest nodeid**: `tests/integration/ua1/test_multiple_datasources.py::test_same_underlying_node_two_sources`

## UA-1-4-06 系统位号名重复拒绝

### 前置条件

- 已注册 tagName='tag_A'

### 测试步骤

1. 在 ds-B 下注册 tagName='tag_A'（与已有重名）

### 预期结果

- 被拒绝，报错位号名重复

**pytest nodeid**: `tests/integration/ua1/test_multiple_datasources.py::test_duplicate_system_tag_name_rejected`

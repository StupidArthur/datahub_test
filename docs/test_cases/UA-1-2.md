<!--
此文件由 pytest 测试代码自动生成。
禁止直接修改。
-->

# UA-1-2

## UA-1-2-01 禁用运行中数据源

### 前置条件

- mock 配置 change=true；数据源 alive=true，位号正常采集

### 测试步骤

1. 连续 getRTValue 2 次，确认值在变化
2. change_ds_state(enabled=false)
3. 等待
4. 检查 alive 和 RT

### 预期结果

- alive=false
- RT 查询抛 TptAPIError(tag 不存在)

**pytest nodeid**: `tests/integration/ua1/test_lifecycle_control.py::test_disable_running_datasource`

## UA-1-2-02 禁用后位号 RT 状态

### 前置条件

- 数据源已禁用

### 测试步骤

1. 禁用数据源
2. getRTValue 读原位号

### 预期结果

- RT 查询抛 TptAPIError
- 错误信息指示 tag 不存在

**pytest nodeid**: `tests/integration/ua1/test_lifecycle_control.py::test_rt_state_after_disable`

## UA-1-2-03 禁用后历史不再增长

### 前置条件

- mock 配置 change=true；数据源 alive=true；采集稳定一段时间

### 测试步骤

1. 等待若干采集周期，确认历史基线 > 0
2. 记录禁用动作时间 t_disable
3. change_ds_state(enabled=false)
4. 等待 alive=false + DataHub 异步落库宽限期
5. 记录稳定时间点 t_stable
6. 在 [t_stable, t_stable + N] 窗口反复查询历史
7. 期望：窗口内历史条数为 0 或不增长

### 预期结果

- 禁用之后历史窗口内不再有新采集点
- 已有历史保留

**pytest nodeid**: `tests/integration/ua1/test_history_lifecycle.py::test_history_stops_after_disable`

## UA-1-2-04 重新启用已禁用数据源

### 前置条件

- mock 仍在运行；数据源已禁用

### 测试步骤

1. 禁用数据源
2. change_ds_state(enabled=true)
3. 等待 alive
4. 连续 getRTValue 确认值变化

### 预期结果

- alive=true
- RT quality 恢复非0
- 采集值恢复变化

**pytest nodeid**: `tests/integration/ua1/test_lifecycle_control.py::test_reenable_disabled_datasource`

## UA-1-2-05 启用后历史恢复增长

### 前置条件

- mock 配置 change=true；数据源已禁用；mock 仍在运行

### 测试步骤

1. 禁用数据源，等待离线稳定
2. 记录重新启用动作时间 t_re_enable
3. change_ds_state(enabled=true)
4. 等待 alive=true + RT quality 恢复
5. 在 [t_re_enable, now] 窗口轮询历史
6. 期望：新点开始出现

### 预期结果

- 启用之后历史窗口起点之后有新采集点

**pytest nodeid**: `tests/integration/ua1/test_history_lifecycle.py::test_history_resumes_after_enable`

## UA-1-2-06 重复启用

### 前置条件

- 已 alive=true

### 测试步骤

1. change_ds_state(enabled=true)

### 预期结果

- 无异常
- 状态保持 alive=true

**pytest nodeid**: `tests/integration/ua1/test_lifecycle_control.py::test_repeat_enable`

## UA-1-2-07 重复禁用

### 前置条件

- 已 alive=false

### 测试步骤

1. 禁用数据源
2. 再次 change_ds_state(enabled=false)

### 预期结果

- 无异常
- 状态保持 alive=false

**pytest nodeid**: `tests/integration/ua1/test_lifecycle_control.py::test_repeat_disable`

## UA-1-2-08 多次启停循环

### 前置条件

- mock 配置 change=true；数据源正常

### 测试步骤

1. 禁用->等->启用->等->禁用->等->启用
2. 每步验证状态和 RT

### 预期结果

- 每次状态正确切换
- 禁用时 RT 查询抛 TptAPIError
- 启用时 quality 恢复非0
- 最终 alive=true

**pytest nodeid**: `tests/integration/ua1/test_lifecycle_control.py::test_multiple_start_stop_cycles`

<!--
此文件由 pytest 测试代码自动生成。
禁止直接修改。
-->

# UA-1-1

## UA-1-1-01 正常连接(URL 无 path)

### 前置条件

- mock 已启动，端口可达

### 测试步骤

1. add_ds_info(url=opc.tcp://ip:port)，不带 path
2. 启用数据源
3. 创建一次位号绑定 mocker 节点
4. 等待实时值出现
5. 验证实时值与质量码

### 预期结果

- alive=true
- getRTValue 返回有效值
- quality 有效(非0)

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_normal_connection_no_path`

## UA-1-1-02 正常连接(URL 有 path)

### 前置条件

- mock 已启动，endpoint 带 path

### 测试步骤

1. add_ds_info(url=opc.tcp://ip:port/path)
2. 启用数据源
3. 创建位号
4. 等待实时值

### 预期结果

- alive=true
- getRTValue 返回有效值

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_normal_connection_with_path`

## UA-1-1-03 两种 URL 格式区别

### 前置条件

- 同一 mock server

### 测试步骤

1. 用 opc.tcp://ip:port 注册数据源 A
2. 用 opc.tcp://ip:port/path 注册数据源 B
3. 都启用
4. 各注册位号并读取 RT

### 预期结果

- 两条数据源都 alive=true
- 位号独立采集

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_two_url_formats`

## UA-1-1-04 不可达地址

### 前置条件

- 目标端口未监听

### 测试步骤

1. 获取动态空闲端口(确保未监听)
2. add_ds_info 指向该端口
3. 启用数据源
4. 观察窗口内持续检查 alive

### 预期结果

- alive=false
- 系统不崩溃

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_unreachable_address`

## UA-1-1-05 不可达变可达

### 前置条件

- 数据源已配置但 alive=false

### 测试步骤

1. 获取动态空闲端口
2. 创建数据源指向该端口(未启动)
3. 启用，确认 alive=false
4. 在同一端口启动 mocker
5. 轮询 alive 变 true
6. 验证位号 RT 值出现
7. 停止 mocker，清理

### 预期结果

- alive 从 false 变 true
- RT 值出现

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_offline_to_online`

## UA-1-1-06 数据源有鉴权，不配凭据

### 前置条件

- mock 配置用户名密码鉴权

### 测试步骤

1. 启动带认证的 mocker
2. add_ds_info 不带凭据
3. 启用数据源
4. 等待观察 alive

### 预期结果

- alive=false（鉴权失败）

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_auth_required_no_creds`

## UA-1-1-07 数据源有鉴权，配正确凭据

### 前置条件

- mock 配置用户名密码鉴权

### 测试步骤

1. 启动带认证的 mocker
2. add_ds_info 带正确 dsExtInfo 凭据
3. 启用数据源
4. 创建位号，验证 RT

### 预期结果

- alive=true
- getRTValue 返回值正确

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_auth_correct_creds`

## UA-1-1-08 数据源无鉴权，配了凭据

### 前置条件

- mock 无鉴权

### 测试步骤

1. add_ds_info 带多余 dsExtInfo 凭据
2. 启用数据源
3. 创建位号，验证 RT

### 预期结果

- alive=true（多余凭据不影响连接）
- getRTValue 返回值正确

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_no_auth_extra_creds`

## UA-1-1-09 不配好值质量码

### 前置条件

- mock 已启动

### 测试步骤

1. add_ds_info 不设好值质量码
2. 启用，创建位号
3. 读取 RT quality

### 预期结果

- RT quality 为平台默认好值(192)
- 值正常采集

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_quality_default`

## UA-1-1-10 配置正常好值(192)

### 前置条件

- mock 已启动

### 测试步骤

1. add_ds_info 好值质量码=192
2. 启用，创建位号
3. 读取 RT quality

### 预期结果

- RT quality=192
- 值正常采集

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_quality_192`

## UA-1-1-11 配置非标准好值(如 0)

### 前置条件

- mock 已启动

### 测试步骤

1. add_ds_info 好值质量码=0
2. 启用，创建位号
3. 读取 RT quality 和采集行为

### 预期结果

- quality=0 的值被系统视为好值正常采集
- RT 有值

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_quality_zero`

## UA-1-1-12 重复地址注册

### 前置条件

- 已有数据源指向 url-A

### 测试步骤

1. 创建数据源 A 指向 mocker endpoint
2. 再次 add_ds_info 使用相同 url-A

### 预期结果

- 第二次请求抛出 TptAPIError
- 错误码为 A0001
- 错误信息包含 Duplicate

**pytest nodeid**: `tests/integration/ua1/test_connection_establishment.py::test_duplicate_url_rejected`
